{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/zoltankiss/agent-driven-development/schemas/add-manifest.schema.json",
  "title": "ADD Discovery Manifest",
  "description": "Schema for the /.well-known/add.json discovery manifest served by ADD-native apps.",
  "type": "object",
  "required": ["add_version", "app_name", "auth"],
  "properties": {
    "add_version": {
      "type": "string",
      "description": "The ADD specification version this app implements (semver).",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[0-9A-Za-z.-]+)?$",
      "examples": ["1.0.0"]
    },
    "app_name": {
      "type": "string",
      "description": "Human/agent-readable name of the application."
    },
    "app_description": {
      "type": "string",
      "description": "Brief description of what this app does."
    },
    "spec_url": {
      "type": "string",
      "format": "uri",
      "description": "URL to the ADD specification."
    },
    "documentation_url": {
      "type": "string",
      "description": "Path or URL to the app's API documentation."
    },
    "openapi_url": {
      "type": "string",
      "description": "Path or URL to the app's OpenAPI specification."
    },
    "auth": {
      "type": "object",
      "required": ["agent_signup"],
      "properties": {
        "protocol": {
          "type": "string",
          "description": "Auth protocol identifier. MUST be 'web-bot-auth' in ADD 1.0.",
          "enum": ["web-bot-auth"]
        },
        "agent_signup": {
          "type": "string",
          "description": "Path to the agent signup endpoint. Signup requests MUST be signed per Web Bot Auth (RFC 9421)."
        },
        "profile_url": {
          "type": "string",
          "description": "Path to view/update the current user's profile."
        },
        "session_url": {
          "type": "string",
          "description": "Optional. Path to mint an opportunistic Bearer token from a signed request."
        },
        "human_passkey_register": {
          "type": "string",
          "description": "Path to the WebAuthn/passkey registration options endpoint."
        },
        "human_passkey_login": {
          "type": "string",
          "description": "Path to the WebAuthn/passkey authentication options endpoint."
        },
        "human_oauth_providers": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of supported OAuth providers for human authentication."
        }
      },
      "additionalProperties": false
    },
    "platform_directory_url": {
      "type": "string",
      "format": "uri",
      "description": "URL to the platform's RFC 9421 key directory (JWK Set). Used by agents to verify platform-signed webhooks."
    },
    "platform_public_key": {
      "type": "object",
      "description": "Platform's Ed25519 public key as a JWK. Bootstrap shortcut for agents that want TOFU pinning instead of fetching the directory.",
      "required": ["kty", "crv", "x"],
      "properties": {
        "kty": { "const": "OKP" },
        "crv": { "const": "Ed25519" },
        "x": { "type": "string", "description": "base64url-encoded public key" },
        "kid": { "type": "string", "description": "JWK Thumbprint (RFC 7638)" }
      }
    },
    "notifications": {
      "type": "object",
      "properties": {
        "webhook_config": {
          "type": "string",
          "description": "Path to configure webhook notification settings."
        }
      },
      "additionalProperties": false
    },
    "feedback_url": {
      "type": "string",
      "description": "Path to the feedback endpoint."
    },
    "sitemap_url": {
      "type": "string",
      "description": "Path to the full sitemap endpoint."
    }
  },
  "additionalProperties": false
}
